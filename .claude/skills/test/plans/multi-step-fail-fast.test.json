{
  "schemaVersion": "1",
  "name": "multi-step-fail-fast",
  "description": "Demonstrates fail-fast envelope shape. Step A passes, step B fails with a non-zero exit, step C never runs because step B stopped the plan. The final assertion is also skipped. envelope.failure.stage is 'step' and failure.reason names step B. Use this when authoring a new plan to confirm your understanding of failure semantics.",
  "steps": [
    {
      "kind": "shell",
      "id": "step-a-pass",
      "command": "node",
      "args": ["-e", "process.stdout.write('A ok')"],
      "timeoutMs": 5000
    },
    {
      "kind": "shell",
      "id": "step-b-fail",
      "command": "node",
      "args": ["-e", "process.stderr.write('B intentional failure'); process.exit(3)"],
      "timeoutMs": 5000
    },
    {
      "kind": "shell",
      "id": "step-c-never-runs",
      "command": "node",
      "args": ["-e", "process.stdout.write('C')"],
      "timeoutMs": 5000
    },
    {
      "kind": "assert",
      "id": "a-says-ok",
      "assertion": "step-output-contains",
      "config": {
        "stepId": "step-a-pass",
        "stream": "stdout",
        "text": "A ok"
      }
    }
  ]
}
