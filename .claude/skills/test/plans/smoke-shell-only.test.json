{
  "schemaVersion": "1",
  "name": "smoke-shell-only",
  "description": "Trivial smoke plan. Verifies bankai is wired and the orchestrator round-trips a single shell step + step-output assertion. Adapt by replacing the command and the expected substring.",
  "steps": [
    {
      "kind": "shell",
      "id": "say-hello",
      "command": "node",
      "args": ["-e", "process.stdout.write('hello bankai world')"],
      "timeoutMs": 10000
    },
    {
      "kind": "assert",
      "id": "stdout-has-hello",
      "assertion": "step-output-contains",
      "config": {
        "stepId": "say-hello",
        "stream": "stdout",
        "text": "hello bankai"
      }
    }
  ]
}
