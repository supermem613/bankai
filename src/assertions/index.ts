// Assertion kind index. Importing this module triggers registration of every
// built-in assertion kind via the _registry side effect on each file. Adding
// a new assertion kind requires a new file in src/assertions/ and a new
// import below.

import "./step-output-contains.js";
